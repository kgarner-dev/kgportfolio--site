<main class="hero">
    <div class="hero-headline">
        <h1>Kaleb Garner</h1>
        <h2>Web Developer</h2>
    </div>

    <div class="hero-text">
        <p>Holistic Full Stack Developer in HealthTech. Focused on creating high-quality solutions to enhance the medical field. Started as a front-end developer and designer, specializing in patient acquisition and practice branding. Now developing full-stack applications to assist medical practitioners in the independent dispute resolution process.</p>
    </div>
</main>

<style lang="scss">
    @use './global.scss';

    .hero {
        max-width: 1400px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        grid-template-rows: auto;
        gap: 1rem;
        padding: 25px;

        @media (min-width: 600px) {
            min-height: 45vh;
            padding: 45px;
        }

        &-headline, &-text {
            grid-column: auto / span 2;
            position: relative;
            z-index: 5;

            @media (min-width: 600px) {
                grid-column: auto / span 1;
            }
        }

        &-headline {
            h1 {
                margin: 0 0 10px;
                font-size: 2.85rem;
                display: flex;
                gap: 10px;

                &::before {
                    height: 100%;
                    aspect-ratio: 1;
                    content: '';
                    background: url('/doodle.png') center center/cover no-repeat;
                    border-radius: 50%;
                }
            }

            h2 {
                margin: 0;
                font-size: 1.65rem;
            }

            @media (min-width: 600px) {
                display: flex;
                flex-direction: column;
                align-items: flex-start;
                justify-content: flex-start;
            }

        }

        &-text {
            position: relative;

            @media (min-width: 600px) {
                display: flex;
                flex-direction: column;
                align-items: flex-start;
                justify-content: flex-end;

                &::after {
                    inset: 0;
                    position: absolute;
                    content: '';
                    background: url('/logo.png') bottom center/contain no-repeat;
                    z-index: 1;
                    opacity: 0.085;
                }
            }

            * {
                position: relative;
                z-index: 5;
            }
        }

        h1, h2, p {
            color: var(--white);
        }
    }
</style>